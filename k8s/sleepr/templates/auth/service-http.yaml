apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth
  name: auth-http
spec:
  ports:
    - name: 'http'
      port: 4001
      protocol: TCP
      targetPort: 4001
  selector:
    app: auth
  type: NodePort
